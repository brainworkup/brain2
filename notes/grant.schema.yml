version: 1
imports: [project]
schemas:
  - id: grant
    title: grant
    desc: grants related to project
    parent: root
    pattern: grant
    namespace: true
    template:
      id: templates.grant
      type: note
    children:
      - aims

  - id: aims
